import React from 'react'
import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const HelpPage = () => {
  return (
    <SafeAreaView>
      <Text style={{ color: '#FFFFFF' }}>HelpPage</Text>
    </SafeAreaView>
  )
}

export default HelpPage